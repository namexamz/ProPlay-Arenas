# Система бронирования площадок

Это групповой проект, выполненный студентами школы программирования.

## 🌎 Сайт проекта

[Ссылка на демо-версию](http://localhost:8085)

## Функционал сайта

- Авторизация и регистрация пользователей
- Просмотр списка площадок с фильтрацией по району, типу и цене
- Поиск площадки по названию
- Просмотр детальной информации о площадке
- Бронирование площадки на определенное время
- Проверка доступности площадки
- Управление расписанием площадок (для владельцев)
- Обработка платежей и возвратов
- Просмотр истории бронирований
- Получение сводной информации о бронировании
- Управление площадками (создание, редактирование, удаление для владельцев)
- Стать владельцем площадки

## Запуск проекта

Для запуска проекта вам необходимо набрать команду в терминале:

```bash
docker-compose up --build
```

Или запустить в фоновом режиме:

```bash
docker-compose up -d --build
```

После запуска API будет доступен по адресу: `http://localhost:8085`

Для остановки всех сервисов:

```bash
docker-compose down
```

### Требования

- Docker Desktop (или Docker Engine + Docker Compose)
- Минимум 4GB свободной RAM

### Переменные окружения

Создайте файл `.env` в корне проекта (опционально):

```env
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production
```

## Технологии проекта

Go (Golang) Gin GORM PostgreSQL Kafka Zookeeper JWT Docker Docker Compose API Gateway Микросервисная архитектура

## Команда проекта

- [namexamz](https://github.com/namexamz)

## В этом проекте я выполнял следующие задачи:

- Создать микросервисную архитектуру на Go
- Реализовать API Gateway для маршрутизации запросов
- Настроить аутентификацию и авторизацию через JWT
- Организовать взаимодействие между микросервисами через Kafka
- Реализовать сервис управления пользователями
- Реализовать сервис управления площадками
- Реализовать сервис бронирований
- Реализовать сервис обработки платежей
- Настроить Docker Compose для оркестрации всех сервисов
- Создание и контроль веток git

Создал проект с микросервисной архитектурой, разделив функционал на отдельные сервисы: Gateway, User Service, Venue Service, Reservation Service и Payment Service.

Для каждого сервиса настроил отдельную базу данных PostgreSQL и организовал асинхронную коммуникацию между сервисами через Kafka для обработки событий бронирования и платежей.

Установил и настроил следующие технологии:
- Go 1.25 с фреймворком Gin для HTTP API
- GORM для работы с базами данных
- Kafka и Zookeeper для message broker
- Docker и Docker Compose для контейнеризации
- JWT для аутентификации

Для выполнения поставленных задач я использовал следующие библиотеки и инструменты:
- gin-gonic/gin - для создания HTTP API
- gorm.io/gorm - для работы с PostgreSQL
- golang-jwt/jwt - для JWT токенов
- confluent-kafka-go - для работы с Kafka
- docker-compose - для оркестрации сервисов
